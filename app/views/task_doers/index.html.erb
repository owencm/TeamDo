<h1>Listing task_doers</h1>

<table>
  <tr>
    <th>User</th>
    <th>Task</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @task_doers.each do |task_doer| %>
  <tr>
    <td><%= task_doer.user_id %></td>
    <td><%= task_doer.task_id %></td>
    <td><%= link_to 'Show', task_doer %></td>
    <td><%= link_to 'Edit', edit_task_doer_path(task_doer) %></td>
    <td><%= link_to 'Destroy', task_doer, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task doer', new_task_doer_path %>
